/**
 * Recipe is a reusable multi-component block
 * smaller than a Pattern, used heavily by the mock/LLM composer.
 */

export type {
  RecipeId,
  RecipeComponentRef,
  RecipeRegistryEntry as RecipeDefinition,
  PatternSelection,
  GenerateResult,
  ResolvedScreen,
} from "./registry";
